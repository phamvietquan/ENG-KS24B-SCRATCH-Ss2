#include <stdio.h>

int main(){
	int a = 29;
	int b = 8;
	int tong = a+b;
	int hieu = a-b;
	int tich = a*b;
	int thuong = a/b;
	printf(" tong hai so la %d \n", tong);
	printf(" tong hai so la %d \n", hieu);
	printf(" tong hai so la %d \n", tich);
	printf(" tong hai so la %d", thuong);
	return 0;
}
